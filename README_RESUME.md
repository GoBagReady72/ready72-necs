# Ready72 — Project Resume & Continuity (NE Coastal Southeast)

**Milestone:** M0 — Foundation (in progress)  
**Branch:** sandbox  
**Seed:** S1 (canonical QA)  
**Next:** Implement global state reducer + Slice A scaffold

## How to Run
1) `npm install`
2) `npm run dev`
3) Open http://localhost:5173 and check the console for the seed log.
4) Ensure no console errors. That passes M0 visual smoke test.

## Notes
- Tailwind brand color is configured as `ready72: #FF6A00` in `tailwind.config.ts`.
- Minimal HUD + PersonaCard stubs are present; full contracts land in M1+.